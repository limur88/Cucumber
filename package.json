{
  "name": "Cucumber",
  "type": "module",
  "devDependencies": {
    "@wdio/allure-reporter": "^9.0.4",
    "@wdio/cli": "^9.0.4",
    "@wdio/cucumber-framework": "^9.0.4",
    "@wdio/local-runner": "^9.0.4",
    "@wdio/spec-reporter": "^9.0.4"
  },
  "scripts": {
    "test": "wdio run ./wdio.conf.ts",
    "report": "allure generate allure-results -o allure-report --clean",
    "report-open": "npm run report && allure open allure-report"
  }
}
